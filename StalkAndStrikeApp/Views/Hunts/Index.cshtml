@model IEnumerable<StalkAndStrikeApp.Models.Hunt>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.UserId)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Location)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.GameTracked)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.UserId)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Location)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.GameTracked)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.HuntId">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.HuntId">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.HuntId">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
